import json
import swaps

new_value = swaps.value
new_CLEAN = swaps.ClEAN
new_NOISY = swaps.NOISY

def imported_data(arg):
    while arg == new_value:
        print()

(" https://realpython.com/python-json/#a-very-brief-history-of-json ")